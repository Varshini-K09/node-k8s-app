apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-k8s-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-k8s-app
  template:
    metadata:
      labels:
        app: my-k8s-app
    spec:
      containers:
      - name: my-k8s-app
        image: kicbase/stable:v0.0.50/my-k8s-app:7  # your Docker Hub image
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
